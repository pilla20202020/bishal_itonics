{#
/**
 * @file
 * Theme implementation of a views exposed form for multiselect dropdown tests.
 *
 * Print the form actions first to ensure that they are always clickable in
 * tests, as open dialogs appear over the submit button some tests.
 */
#}
{% if q is not empty %}
  {#
  This ensures that, if clean URLs are off, the 'q' is added first,
  as a hidden form element, so that it shows up first in the POST URL.
  #}
  {{ q }}
{% endif %}
{{ form.actions }}
{{ form|without('actions') }}
